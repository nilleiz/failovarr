# 0.7.0 public product extraction gate

| ID | Requirement | Implementation | Acceptance evidence | Status |
| --- | --- | --- | --- | --- |
| P700-01 | Public product repository contains no private history, production data or host details. | Clean repository root with a public allowlist; private lab remains separate. | Boundary scan and PR review. The initial contract run exposed only a validator self-match, not an actual leak. | implemented; CI correction in progress |
| P700-02 | Product is named Failovarr and is visibly WIP/vibe-coded. | Rename package, manifest, logs, assets, docs and release naming; add public notices. | Manifest and documentation contract. | implemented; pending CI |
| P700-03 | Existing 0.6.17 installations can migrate safely. | Accept legacy profile format, preserve the legacy configuration file and its owner/group during an atomic node-local migration, retain old state path, block concurrent plugin identities. | Unit and synthetic root-installer/dispatch-worker migration tests plus operator migration acceptance. | implemented; CI and operator migration pending |
| P700-04 | Public repo supports future Dispatcharr registry submission. | MIT, manifest URLs, logo, community files and registry-readiness checklist. | Static contract and public review. | implemented; CI pending |
| P700-05 | Public docs include feature status, Wiki sources, Getting Started and security/contribution guidance. | Add feature catalogue, public Wiki sources and community files. | Repository contract. | implemented; CI pending |
| P700-06 | CI selects relevant suites and requires documented test impact; release candidates always fully qualify. | Change classifier, PR template, aggregate check and exact-commit release gate. | Initial run selected full qualification correctly but wrote malformed multi-output values, so dependent suites were skipped; regression correction is in progress. | implemented; CI correction in progress |
| P700-08 | An unreleased public Draft-PR retains its planned candidate version across CI-only corrections. | State the rule in public agent guidance and retain `0.7.0` for this correction. | Documentation review and subsequent CI run. | implemented; CI pending |
| P700-09 | A root-triggered plugin discovery must not make migrated node-local configuration unreadable by Dispatcharr. | Carry ownership from the legacy file (or existing Failovarr file) into the atomic replacement; cover the actual root-installer then dispatch-worker path. | Package ownership regression and synthetic cluster migration probe; operator re-runs non-destructive lab upgrade. | implemented; CI and operator migration pending |
| P700-10 | A green release workflow must publish a tagged pre-release with verified assets, or fail without claiming success. | Address the draft release by release ID, use authenticated upload endpoints, make every API failure fatal, and verify draft/prerelease/tag/asset/digest/download state. | Release contract regression, full qualification, then a resumed `v0.7.0` GitHub release workflow. | implemented; CI and release verification pending |
| P700-07 | The existing workspace becomes a private human/AI laboratory with the public product as submodule. | Rename private remote; replace duplicate product tree with `product/failovarr`; retarget lab scripts/docs. | Operator-approved migration review after public PR. | deferred until public qualification and operator migration acceptance |
